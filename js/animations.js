document.addEventListener('DOMContentLoaded', () => {
  // The current page uses the intro and reveal flow from main.js.
  // This script is intentionally kept lightweight so the experience remains stable.
});
